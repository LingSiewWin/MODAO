# Copy to .env and fill in. Never commit .env.
# Deployer key — must be funded with >=10 MON on Monad testnet (reserve balance floor).
PRIVATE_KEY=0xYOUR_PRIVATE_KEY_HERE

# Optional. Verification on Monad block explorer.
MONAD_EXPLORER_API_KEY=
